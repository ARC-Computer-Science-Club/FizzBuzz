#include "Buzz.h"
const int BUZZ_WORTHY = 5;

/*
  Contructor for Buzz class. This should set response to "Buzz".
  Uses the ZZ class to get the last two letters of "Buzz"
*/
Buzz::Buzz() 
: ZZ() {

}

/*
  This function will check if i is divisible by the BUZZ_WORTHY number
  @parameter: int i is the number we are comparing to BUZZ_WORTHY
  @return: boolean value true if i is divisible by BUZZ_WORTHY else false.
*/
bool Buzz::isWorthy(int i) {
  
}